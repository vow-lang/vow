[package]
name = "vow-verify"
version = "0.3.0"
edition = "2024"

[dependencies]
vow-ir = { path = "../vow-ir" }
vow-diag = { path = "../vow-diag" }
tempfile = "3"
libc = "0.2"

[dev-dependencies]
vow-syntax = { path = "../vow-syntax" }
