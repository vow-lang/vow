// TEST: stdout "60"
module ForEach

fn main() -> i32 [io] {
    let v: Vec<i64> = Vec::new();
    v.push(10);
    v.push(20);
    v.push(30);

    let mut total: i64 = 0;
    for x in v {
        total = total + x;
    }
    print_i64(total);
    0
}
