// TEST: stdout "7"
module VecNarrowIndexBinopIf

fn main() -> i32 [io] {
    let v: Vec<u32> = Vec::new();
    v.push(7u32);
    let mut s: u32 = 0;
    let mut j: i64 = 0;
    if j < 1 {
        s = s + v[j];
        j = j + 1;
    }
    print_u64(s as u64);
    0
}
