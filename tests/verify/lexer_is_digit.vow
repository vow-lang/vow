// TEST: category model-drift
module LexerIsDigitVerify

fn is_digit(b: i64) -> bool vow {
    requires: b >= 0,
    requires: b <= 255,
    ensures: result == (b >= 48 && b <= 57)
} {
    b >= 48 && b <= 57
}

fn main() -> i32 {
    if is_digit(48) { 0 } else { 1 }
}
