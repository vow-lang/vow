// TEST: category callee-blame
module Max

fn max_of(a: i64, b: i64) -> i64 vow {
  ensures: result >= a,
  ensures: result >= b
} {
  if a > b {
    a
  } else {
    b
  }
}

fn main() -> i32 [io] {
  print_i64(max_of(3, 7));
  print_i64(max_of(-1, -5));
  0
}
