// TEST: counterexample-fn "safe_sub"
// TEST: counterexample-blame callee
module CegisBroken

fn safe_sub(a: i64, b: i64 where b >= 0) -> i64 vow {
  ensures: result >= 0
} {
  a - b
}

fn main() -> i32 [io] {
  print_i64(safe_sub(10, 3));
  0
}
