// TEST: error-code ShiftCountOutOfRange
// TEST: stderr "shift count -1 is out of range for u32"
module U32NegativeShiftCount

fn main() -> i32 {
    let value: u32 = 1;
    let shifted: u32 = value << -1;
    0
}
