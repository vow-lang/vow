module BreakValue

fn first_big(n: i64) -> i64 {
    let mut i: i64 = 0;
    loop {
        if i > n {
            break if i > 10 { i } else { i + 1 };
        }
        i = i + 1;
    }
}

fn main() -> i64 {
    first_big(3)
}
