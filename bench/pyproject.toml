[project]
name = "bench"
version = "0.1.0"
description = "Vericoding benchmark runner for Vow"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.52.0",
    "openai>=1.82.0",
]

[project.scripts]
bench = "run:main"
