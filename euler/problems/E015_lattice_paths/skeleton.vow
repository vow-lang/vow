module LatticePaths

fn lattice_paths(n: i64) -> i64 vow {
  requires: n >= 0,
  requires: n <= 30,
  ensures: result >= 1
} {
  1
}

fn main() -> i32 [io] {
  let answer: i64 = lattice_paths(20);
  print_i64(answer);
  0
}
