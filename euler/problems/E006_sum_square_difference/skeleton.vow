module SumSquareDifference

fn sum_of_squares(n: i64) -> i64 vow {
  requires: n >= 0,
  ensures: result >= 0
} {
  0
}

fn square_of_sum(n: i64) -> i64 vow {
  requires: n >= 0,
  ensures: result >= 0
} {
  0
}

fn difference(n: i64) -> i64 vow {
  requires: n >= 1,
  ensures: result >= 0
} {
  0
}

fn main() -> i32 [io] {
  let answer: i64 = difference(100);
  print_i64(answer);
  0
}
