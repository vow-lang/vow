module PowerOfTwo

fn pow2(n: i64) -> i64 vow {
  requires: n >= 0,
  requires: n <= 8,
  ensures: result >= 1
} {
  0
}

fn main() -> i32 [io] {
  print_i64(pow2(0));
  print_i64(pow2(3));
  print_i64(pow2(8));
  0
}
