module FibonacciBounded

fn fib(n: i64) -> i64 vow {
  requires: n >= 0,
  requires: n <= 8,
  ensures: result >= 0
} {
  0
}

fn main() -> i32 [io] {
  print_i64(fib(0));
  print_i64(fib(5));
  print_i64(fib(8));
  0
}
