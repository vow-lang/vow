module CountSteps

fn count_steps(n: i64) -> i64 vow {
  requires: n >= 0,
  requires: n <= 8,
  ensures: result == n
} {
  0
}

fn main() -> i32 [io] {
  print_i64(count_steps(5));
  print_i64(count_steps(0));
  0
}
