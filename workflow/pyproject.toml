[project]
name = "workflow"
version = "0.1.0"
description = "Multi-agent workflow orchestrator for Vow development"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.52.0",
    "openai>=1.82.0",
]

[project.scripts]
workflow = "run:main"
