[package]
name = "vow-runtime"
version = "0.2.0"
edition = "2024"

[lib]
crate-type = ["staticlib", "rlib"]

[dependencies]
vow-diag = { path = "../vow-diag" }
serde_json = "1"
libc = "0.2"
flate2 = "1"
