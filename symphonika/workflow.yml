workflow:
  name: vow_plan_tdd_pr
  initial: plan

  states:
    # Inlined from builtin:plan-tdd-pr because `with:` overrides template
    # inputs, not transitions, and this pipeline needs stricter gates than the
    # template's on both stages. The template's planning gate is
    # `provider_success` + `artifact_exists` (symphonika#586) and its
    # implementing gate is `branch_ahead_of_base` alone; both are reproduced
    # below with an added per-attempt progress requirement.
    plan:
      action:
        kind: agent
        provider: claude
        prompt: prompts/plan.md
      transitions:
        # artifact_exists probes the Run Workspace for the plan file itself, so
        # a planner that commits something other than a plan cannot pass.
        # branch_advanced_since_attempt_start is false unless this run added a
        # commit, so a resumed attempt cannot coast on a PLAN.md an earlier
        # attempt left in the workspace.
        - to: implement
          when:
            provider_success: true
            artifact_exists: PLAN.md
            branch_advanced_since_attempt_start: true
        - to: failed

    implement:
      action:
        kind: agent
        provider: claude
        prompt: prompts/impl.md
      transitions:
        # branch_ahead_of_base alone would be satisfied by the plan commit
        # already on the branch, so require this run to advance HEAD too.
        # This only proves the agent committed something -- it says nothing
        # about whether it ever pushed and opened the PR prompts/impl.md tells
        # it to. Issue #1277's implement run backgrounded the quality gate,
        # ended its turn waiting on it, and never reached the "Open the PR"
        # step -- yet still satisfied every predicate below. wait_for_pr_open
        # closes that gap; mirrors symphonika's own workflow.yml, which added
        # the identical state for the same reason (issue #730, #1255).
        - to: wait_for_pr_open
          when:
            provider_success: true
            branch_ahead_of_base: true
            branch_advanced_since_attempt_start: true
        - to: failed

    # pr_signal predicates (pr_open/pr_merged/...) are only projected for
    # kind: wait states -- run-controller's agent-completion path only ever
    # supplies provider_success/branch_ahead_of_base/branch_advanced_since_attempt_start
    # (outcome-projection.ts), so a pr_open check cannot live on implement's own
    # transitions. This dedicated wait state is the supported way to gate on PR
    # existence: it reuses the same tracked-PR polling and the
    # no-PR-ever-tracked escalation to sym:blocked/sym:human-needed that
    # wait_for_pr already relies on, but fails fast right after implement
    # instead of silently burning a code_review_fix and a simplify run against
    # a branch with no PR first.
    wait_for_pr_open:
      action:
        kind: wait
      transitions:
        - to: merged
          when:
            pr_merged: true
        - to: code_review_fix
          when:
            pr_open: true
        - to: failed
          when:
            pr_open: false

    code_review_fix:
      action:
        kind: agent
        provider: claude
        prompt: prompts/code-review-fix.md
      transitions:
        - to: simplify
          when:
            provider_success: true
        - to: failed

    simplify:
      action:
        kind: agent
        provider: claude
        prompt: prompts/simplify.md
      transitions:
        - to: wait_for_pr
          when:
            provider_success: true
        - to: failed

    wait_for_pr:
      action:
        kind: wait
      transitions:
        - to: merged
          when:
            pr_merged: true
        - to: failed
          when:
            pr_open: false
        - to: merge
          when:
            checks: success
            mergeable: true
            unresolved_review_threads: 0
        - to: resolve_conflicts
          when:
            mergeable: false
        - to: autofix
          when:
            checks: failure
        # Reviewer feedback on an otherwise-green PR. Ordered last so a clean
        # PR still merges and a broken one still repairs first. Without this
        # the state parks forever on the common shape -- green checks, one
        # unresolved thread -- because `merge` needs
        # unresolved_review_threads: 0 and `autofix` needs failing checks.
        # The orchestrator-wide review_followup loop no longer dispatches
        # alongside a parked raw-FSM run (issue #616), so a case this state
        # does not name is a case nothing handles.
        - to: autofix
          when:
            has_unresolved_reviews: true

    autofix:
      action:
        kind: agent
        provider: claude
        prompt: prompts/autofix-pr.md
      transitions:
        - to: wait_for_pr
          when:
            provider_success: true
        - to: failed

    resolve_conflicts:
      action:
        kind: agent
        provider: claude
        prompt: prompts/resolve-conflicts.md
      transitions:
        - to: wait_for_pr
          when:
            provider_success: true
        - to: failed

    merge:
      action:
        kind: merge_pr
      transitions:
        - to: merged
          when:
            pr_merged: true
        - to: failed
          when:
            pr_open: false
        - to: resolve_conflicts
          when:
            mergeable: false
        - to: autofix
          when:
            checks: failure

    merged:
      terminal: success

    failed:
      terminal: blocked
